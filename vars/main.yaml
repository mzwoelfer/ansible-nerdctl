_nerdctl_packages:
  default:
    - uidmap
    - dbus-user-session
  Debian-11:
    - uidmap
    - dbus-user-session
    - fuse3

nerdctl_packages: >-
  {{ _nerdctl_packages[ansible_distribution ~ '-' ~ ansible_distribution_major_version]
      | default(_nerdctl_packages[ansible_distribution]
      | default(_nerdctl_packages['default']))
  }}
